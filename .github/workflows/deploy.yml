name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 82.25.106.218
          username: u277663530
          password: AlphaNodus@79
          server-dir: /home/u277663530/domains/blueprove.com/public_html/cafeconnects/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
